<Window x:Class="Project2_Code.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:gmaps="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation"
        xmlns:local="clr-namespace:Project2_Code"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <TabControl RenderTransformOrigin="0.5,0.5">
        <TabItem x:Name="Loader" Header="Loader" Height="24" Margin="-2,-2,-17,0" VerticalAlignment="Top">
            <Grid Height="434" Width="800">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="149*"/>
                    <ColumnDefinition Width="651*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="FileButton" Content="CHOOSE FILE" Margin="15,73,164,124" Click="File_Click" Grid.Column="1"/>

            </Grid>
        </TabItem>
        <TabItem x:Name="Radar" Header="Radar" Height="23" Margin="18,-2,-59,0" VerticalAlignment="Top">
            <Grid Background="#FFE5E5E5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Background="#FFFFFFFF" Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="PlayButton" Click="PlayPause_Click" Grid.Column="0" Background="White">
                        <Image Source="{StaticResource playIcon}"/>
                    </Button>
                    <Button x:Name="ResetButton" Content="Reset" Click="Reset_Click" Grid.Column="1"/>
                </Grid>
                <gmaps:GMapControl x:Name="gmap" Loaded="gmapLoaded" Grid.Row="1"/>
            </Grid>
        </TabItem>
    </TabControl>
</Window>
